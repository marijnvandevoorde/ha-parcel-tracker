{
  "config": {
    "step": {
      "user": {
        "title": "Parcel Tracker",
        "description": "Enter your API keys below to enable reliable tracking. Both are free and optional — you can also add them later via the integration settings.\n\n**DHL API Key** (DHL Express + DHL Germany) — Free, 250 calls/day. Register at [{dhl_url}]({dhl_url}), create an app and copy the **Consumer Key**.\n\n**pkge.net API Key** (DPD + 4PX / China Post) — Free, 50 parcels/month. Register at [{pkge_url}]({pkge_url}), go to **Company profile → API key**.",
        "data": {
          "dhl_api_key": "DHL API Key",
          "pkge_api_key": "pkge.net API Key",
          "postal_code": "Destination postal code"
        },
        "data_description": {
          "dhl_api_key": "My Apps → Consumer Key",
          "pkge_api_key": "Company profile → API key",
          "postal_code": "Needed by bpost and Colis Privé to return tracking data (e.g. 9090)"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Parcel Tracker is already installed. Manage your parcels via the sidebar panel."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Parcel Tracker — API Keys",
        "description": "Update your API keys here. Changes take effect immediately.\n\n**DHL API Key** (DHL Express + DHL Germany) — Free, 250 calls/day. [{dhl_url}]({dhl_url}) → My Apps → Consumer Key\n\n**pkge.net API Key** (DPD + 4PX / China Post) — Free, 50 parcels/month. [{pkge_url}]({pkge_url}) → Company profile → API key",
        "data": {
          "dhl_api_key": "DHL API Key",
          "pkge_api_key": "pkge.net API Key",
          "postal_code": "Destination postal code"
        },
        "data_description": {
          "dhl_api_key": "My Apps → Consumer Key",
          "pkge_api_key": "Company profile → API key",
          "postal_code": "Needed by bpost and Colis Privé to return tracking data (e.g. 9090)"
        }
      }
    }
  }
}
